//! WalaStack CLI binary — `walastack`.
//!
//! Phase 1 stub. The CLI will eventually provide `new`, `dev`, `build`, `test`,
//! `deploy`, `doctor`, `logs`, `ai`, and `sync` subcommands.

#![allow(clippy::print_stdout)]

fn main() {
    println!("walastack CLI — Phase 1 placeholder");
}
