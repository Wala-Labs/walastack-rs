//! Minimal walastack-rs example.
//!
//! Phase 1 stub. Once `walastack-app` lands, this will demonstrate the
//! canonical `App::new().route(...).run()` pattern with `#[walastack::main]`
//! and `#[get("/")]` macros.

#![allow(clippy::print_stdout)]

fn main() {
    println!("Hello, WalaStack!");
}
